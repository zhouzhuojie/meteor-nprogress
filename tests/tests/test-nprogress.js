// Generated by CoffeeScript 1.7.1
(function() {
  var assert;

  assert = require('assert');

  suite('NProgress', function() {
    test('Environment Setup', function(done, server, client) {
      client["eval"](function() {
        return Meteor.startup(function() {
          return emit('isNProgressExist', typeof NProgress !== "undefined" && NProgress !== null);
        });
      });
      return client.once('isNProgressExist', function(expectTrue) {
        assert(expectTrue);
        return done();
      });
    });
    return test('NProgress api', function(done, server, client) {
      client["eval"](function() {
        return Meteor.startup(function() {
          return emit('isNProgressAPI', _.every([NProgress.start != null, NProgress.done != null]));
        });
      });
      return client.once('isNProgressAPI', function(expectTrue) {
        assert(expectTrue);
        return done();
      });
    });
  });

}).call(this);
